function estado(element,clase){
	Obtener el identificador del elemento 
	retornar true si clase pertence a las clases CSS3 del identificador,false sino
}
function exonerad(element){
	// Precondicion el elemento tiene la clase CSS3 apro
	Cambiar la claseCSS3 apro por la claseCSS3 exo
	cambiar el estado de aprobacion del elemento por el de exo
	aumentar la cantidad de creditos totales
	actualizar en la pagina el valor de los creditos actuales 
}
function aprob(element){
	//Precondicion el elemento tiene la clase CSS3 podes
	Cambiar la claseCSS3 podes por la clase apro
	cambiar el estado de aprobacion del elemento por el de apro
}
function noExo(element){
	// Precondicion el elemento tiene la clase CSS3 exo
	Cambiar la claseCSS3  por la claseCSS3 podes
	cambiar el estado de aprobacion del elemento por el de exo
	aumentar la cantidad de creditos totales
	actualizar en la pagina el valor de los creditos actuales 
}
function habilitado(element)
function change(element) 